const paineis = [];
const certificados = [];
const leituras = []; // ← novo: para guardar leituras de produção
const creditos = []; // ← novo: para guardar créditos por cliente

module.exports = {
  paineis,
  certificados,
  leituras,
  creditos
};
